[project]
name = "mcp-server-demo"
version = "0.1.0"
description = "Servidor MCP DATAR: Prueba con herramientas ambientales"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.18.0",
]

[project.scripts]
mcp-server-demo = "main:main"

[dependency-groups]
dev = []
